---
title: Welcome
description: A kitchen-sink tour of what strides pages can do.
---

strides lets you combine **prose**, math, code, and interactive diagrams on one page.

## Math

Inline math: $e^{i\pi} + 1 = 0$.

Block math:

$$
\text{softmax}(x)_i = \frac{e^{x_i}}{\sum_j e^{x_j}}
$$

## A table

| Feature               | Status              |
| --------------------- | -------------------- |
| MDX pages              | done                 |
| Dark mode              | done                 |
| Python cells           | done                 |
| Interactive diagrams   | coming in Phase 3    |

## Static code

```python
def softmax(x):
    exps = [v.exp() for v in x]
    total = sum(exps)
    return [v / total for v in exps]
```

This block is never executed — it's just highlighted for reading.

## A live cell

This one runs for real, against your local Python environment:

```python cell
import platform

print(f"hello from a strides cell, running Python {platform.python_version()}")
6 * 7
```
