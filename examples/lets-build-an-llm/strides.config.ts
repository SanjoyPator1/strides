import type { StridesConfig } from '@strides/theme'

const config: StridesConfig = {
  title: "Let's Build an LLM",
  description: 'Build a large language model from scratch, in Python and PyTorch — one part at a time.',
}

export default config
